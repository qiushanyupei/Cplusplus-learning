锁的基本使用场景：对于共享资源，使用前加锁、使用后解锁
人为的代码机制有可能出现遗漏，所以需要一些自动管理锁的工具
RALL原则通俗来讲是在初始化（构造函数）时获取资源，析构函数时释放资源
C++ lock函数可以对多个资源同时加锁，避免不同资源的动态顺序死锁（也称为所顺序反转）
std::lock_guard:构造函数里调用lock)加锁，析构时调用unlock解锁，可有效防止因程序异常或函数提前返回造成的锁泄漏，对互斥锁、递归锁、超时锁、读写锁有效,只能管理单个锁（参数模板只有一个锁参数）
例：std::mutex mutex1;
std::lock_guard<std::mutex> lock(mutex1);

std::scoped_lock可以管理多锁，写法就是lock_guard的多锁版本，使用了死锁避免算法（使用原子化的方式尝试锁定所有互斥量，如果某个锁定失败，则释放已锁定的互斥量，等待一段时间后重试）
