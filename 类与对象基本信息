类中有数据成员和函数成员
类的构造函数中有一种初始化列表写法，初始化的内容算是类的成员变量，其中冒号后大括号前的内容会自动根据参数（如果初始化依赖参数的值）初始化：Circle(int X,int Y,double R):Point(X,Y){函数体}
类的静态成员变量属于类本身，只占用单个存储空间，不会每个对象中都有副本
this指针在非静态成员函数内部使用，this 指针指向调用该成员函数的那个对象本身，this指针不能被显示声明
