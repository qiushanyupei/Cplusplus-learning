在int *a = &b中，首先定义int型指针a，然后&是取地址符，即指针a指向b的地址
在int &a = b中，int&是引用声明，a=b是绑定，此后a就代表b
引用在创建时就需要绑定一个有效对象，不能是空引用，创建后不能更换引用对象，函数可以返回引用
现代C++的智能指针shared_ptr功能：1、自动对对象进行引用计数,引用数为0时自动释放该对象；2、可以创建多个共享指针指向同一个物体（感觉主要是和引用计数的特性配合才有用，可以避免内存泄漏因为正常指针也可以多个指针指向同一个物体）
应用场景：1、多个所有者共享对象所有权；2、跨模块内存管理
